<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kolot.x_servicesbeta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XServicesBeta">
        <activity android:name=".CekPesanan"></activity>
        <activity android:name=".BuatPesanan" />
        <activity android:name=".Pemberitahuan" />
        <activity android:name=".Pesan" />
        <activity android:name=".Home" />
        <activity android:name=".Pengaturan" />
        <activity android:name=".Berita" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>